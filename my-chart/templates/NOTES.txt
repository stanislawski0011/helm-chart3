# NOTES.txt intentionally left blank

Thank you for installing {{ .Chart.Name }}!

Your release "{{ .Release.Name }}" has been deployed.

{{- if .Values.ingress.enabled }}
Ingress URLs:
{{- range $env, $hosts := .Values.ingress.hosts }}
  {{- range $host := $hosts }}
    Host: {{ $host.host }}
    {{- range $path := $host.paths }}
      Path: {{ $path.path }} (Port: {{ $path.port }})
    {{- end }}
  {{- end }}
{{- end }}
{{- else }}
No ingress enabled for this release.
{{- end }}
